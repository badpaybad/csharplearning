using System;
using DayOneNamespace;
using DayTwoNamespace;
using DayThreeNamespace;
using DayFourNamespace;
using DayFiveNamespace;
using DaySixNamespace;
using DaySevenNamespace;
using DayEightNamespace;

namespace ChungnbSolutions
{
    class Program
    {
        static void Main(string[] args)
        {
            // DayOne.Solutions();
            // DayTwo.Solutions();0
            // DaySix.Solutions();
            // DaySeven.Solutions();
            // DayEight.Solutions();
            var r = ProblemOne(4);

            if (r == 1)
            {
                var du = "du";

                var xxx = du == "du" ? "true" : (xxx == "" ? "" : "");

                Console.WriteLine(string.Format("ten toi la: {0}vjlsjflsjkdfjls", du));
                Console.WriteLine($"\" \n \r \t toi ten la {du} sfskldjflsdf {xxx}");

            }
        }

        static int ProblemOne(int n)
        {


        }
    }
}
